#include<stdio.h>
int main(){
    int product,c,value,gender;
    while (c <= 5)
    {
        product *= c;
        ++c;}
    scanf( "%.f", &value );
    if ( gender == 1 )
    puts( "Woman" );
    else
    puts("Man");
       
}